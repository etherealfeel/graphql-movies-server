type Query {
    movies: Movies
}

type Movies {
    page: Int!
    totalPages: Int!
    totalResults: Int!
    results: [Movie!]
}

type Movie {
    id: ID!
    title: String!
    releaseDate: String!
    posterPath: String
    genres: [Genre!]
}

type Genre {
    id: ID
    name: String
}